import * as React from "react"

const style = {
  width: '18px',
  borderTop: '4px solid #494e52',
  marginBottom: '1em',
  display: 'inline-block',
}

const TitleBar = () => (
  <div style={style}/>
)

export default TitleBar